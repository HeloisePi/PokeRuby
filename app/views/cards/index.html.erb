<p style="color: green"><%= notice %></p>


  <h1>Cards</h1>

<div id="cards">
  <% @cards.each do |card| %>
    <div class="align">
      <%= render card %>
      <%= link_to "Show this card", card %>
    </div>
  <% end %>
</div>

<%= link_to "New card", new_card_path %>
